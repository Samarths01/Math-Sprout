import { Shell } from "@/components/shell";

export default function ParentLoading() {
  return (
    <Shell>
      <p className="text-sm text-muted-foreground">Loading parent home…</p>
    </Shell>
  );
}
