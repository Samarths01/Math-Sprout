/**
 * Rules baseline stamped on every attempt and practice session.
 * Offline eval (Signal-owned) compares later policies against this string.
 * Production scoring stays MasteryEstimator; this is not a second scorer.
 */
export const POLICY_VERSION = "rules-v0";
