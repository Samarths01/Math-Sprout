/**
 * Numeric stubs from the build plan. They are config, not schema.
 * Review-week and qualifying-day thresholds can change without a migration.
 */
export const REVIEW_SESSIONS_PER_WEEK = 3;

/** Honest Recommended or Challenge tries required on a local day before streak heat. */
export const QUALIFYING_DAY_MIN_HONEST_ATTEMPTS = 1;
